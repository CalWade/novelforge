"""Novelforge — generic multi-agent novel writing.

Genre is injected via `genres/<id>/`; a project (one novel) is declared
under `projects/<id>/` and references a genre. `src/` is genre-agnostic.
"""
__version__ = "0.3.0"
